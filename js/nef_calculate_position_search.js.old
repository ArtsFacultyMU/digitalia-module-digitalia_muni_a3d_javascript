const RESULTS_PER_PAGE = 40;
let separator = "&";
if (!location.search || location.search.length === 0) {
    separator = "?";
}
// The block is used for both homepage and search page, both are redirected to search/paged
const paged_search_url = location.pathname.replace("homepage", "search") + "/paged" + location.search + separator + "page=";
const params = new URLSearchParams(location.search);
let page_number = params.get("page");
let link;
if (page_number == null) {
    page_number = 0;
}

const table = document.querySelector("table.views-view-table tbody");
const table_rows = table.querySelectorAll("tr");
let items;
let tags;
let counter = 0;
for (let i = 0; i < table_rows.length; i++) {
    link = paged_search_url + "0%2C" + (page_number * RESULTS_PER_PAGE + i);
    table_rows[i].querySelector("td.views-field-field-artefact-id a").href = link;
}
